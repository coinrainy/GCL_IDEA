# Experiment Plan: MEGA-GCL Smoke

- 时间：2026-06-26T16:40:00Z
- Idea：MEGA-GCL / Masked Evidence Group Assignment
- 阶段：kill-smoke planning
- 范围：Cora seed0 only
- 协议：stratified random `1:1:8`; frozen encoder; Logistic Regression evaluator; no test-label thresholding。
- Method status：label-free pretext GCL / masked discriminative SSL；不是 false-negative mining。

## Smoke ID

`ME-M0-001`

## Research Question

Can replacing node-node negatives with masked ego evidence group discrimination improve ordinary node classification under the same frozen Logistic Regression evaluator?

## Systems

| ID | System | Description | Required |
|---|---|---|---|
| M0 | GRACE | original node-node contrastive baseline | yes |
| M1 | GraphMAE-style reconstruction | masked feature reconstruction control | yes |
| M2 | GraphMAE2-lite latent/remask | latent target/remask prediction control | yes |
| M3 | GCMAE-style MAE+CL hybrid | masked reconstruction + contrastive stacking control | yes |
| M4 | VQ/codebook autoencoder | codebook evidence without MEGA discrimination | yes |
| M5 | SwAV/prototype assignment | prototype assignment/balance control | yes |
| M6 | MINT token-corruption contrast | masked neighborhood token corruption control | yes |
| M7 | MEGA feature-only | feature evidence groups only | yes |
| M8 | MEGA structure-only | structural evidence groups only | yes |
| M9 | MEGA degree-only | degree/community shortcut control | yes |
| M10 | MEGA full | feature + structure + residual evidence groups | yes |
| M11 | random/permuted evidence groups | same objective with random/permuted group ids | yes |
| M12 | no-mask evidence classifier | group prediction without masked evidence | yes |
| M13 | matched-parameter GRACE | capacity-matched GRACE | should |

## Metrics

- `test_acc_at_best_val`
- `valid_at_best`
- selected Logistic Regression `C`
- evidence prediction accuracy on train/val nodes without using labels
- effective rank
- alignment / uniformity
- covariance redundancy
- class evidence separation diagnostic after training, using labels only after training
- exact run command
- project commit hash and dirty flag
- config path and split path

## GO Rule

`GO_TO_ME_M1_WITH_CAUTION` only if all hold:

1. M10 beats M0 GRACE and M13 matched-parameter GRACE on Cora seed0 accuracy.
2. M10 beats M1 GraphMAE-style, M2 GraphMAE2-lite, M3 GCMAE-style, M4 VQ/codebook, M5 SwAV/prototype, and M6 MINT controls.
3. M10 beats M11 random/permuted evidence and M12 no-mask evidence classifier.
4. M7/M8/M9 diagnostics indicate which evidence source drives the result; degree-only cannot explain most of the gain.

## KILL Rules

- M10 below GRACE：`KILL_MEGA_AS_WEAKER_THAN_GRACE`。
- M10 matches GraphMAE/GraphMAE2/GCMAE：`KILL_MEGA_AS_MASKED_AUTOENCODER_VARIANT`。
- M10 matches VQ/SwAV/prototype：`KILL_MEGA_AS_CODEBOOK_OR_PROTOTYPE_VARIANT`。
- M10 matches MINT：`KILL_MEGA_AS_TOKEN_CORRUPTION_VARIANT`。
- M11 random/permuted groups matches M10：`KILL_EVIDENCE_GROUP_STORY`。
- M12 no-mask matches M10：`KILL_MASKING_STORY`。
- M10 improves diagnostics but not accuracy：`KILL_PROXY_DIAGNOSTIC_REPEAT`。
- `project_dirty=true` or run command missing：`BLOCK_PROVENANCE_BEFORE_PILOT`。

## Integrity Rules

- Evidence groups must be generated without labels.
- Test labels are only read after training/evaluator selection for reporting.
- No kNN/PPR/CAST relations, positive mining, score fusion, relation smoothing, or train-label probe routing.
- All variants use the same split and frozen Logistic Regression evaluator.

## Next Command

```text
[$experiment-bridge](/root/tools/aris/skills/skills-codex/experiment-bridge/SKILL.md)

实现 MEGA-GCL 的 ME-M0-001 kill-smoke：新增 configs/mega_smoke.yaml 和 scripts/run_mega_smoke.py。只跑 Cora seed0，复用 GRACE split/frozen encoder + Logistic Regression evaluator，比较 M0 GRACE / M1 GraphMAE-style reconstruction / M2 GraphMAE2-lite latent-remask / M3 GCMAE-style MAE+CL hybrid / M4 VQ-codebook autoencoder / M5 SwAV-prototype assignment / M6 MINT token-corruption contrast / M7 MEGA feature-only / M8 MEGA structure-only / M9 MEGA degree-only / M10 MEGA full / M11 random-permuted evidence groups / M12 no-mask evidence classifier / M13 matched-parameter GRACE。必须输出 accuracy、evidence prediction accuracy、codebook/prototype usage、effective rank、alignment/uniformity、covariance redundancy、class evidence separation diagnostic，并在 JSON 中保存 exact run command、commit、dirty flag、config path、split path。不允许使用 test label 生成 evidence group 或选择阈值。
```
