# Idea Discovery Report: PACER-GCL

- 时间：2026-06-26T16:06:34Z
- Skill：`/idea-discovery`
- 触发原因：CAST / BEACON / SPECTRA 三连 no-go 后，重新做 false-negative GCL 机制级 idea discovery。
- 状态：fresh reviewer informed；未实现代码、未跑新实验、未产生性能 claim。
- Fresh scientific reviewer verdict：`PIVOT`，置信度 `0.78`。
- Fresh experiment auditor verdict：`WARN`，允许机制级 pivot，但下一轮 smoke 前必须预注册 controls/kill rules、保存完整 run command，并避免 dirty-run provenance 风险。

## Hard Constraints Applied

1. 不继续 CAST C4/C5。
2. 不继续 BEACON gate 或 threshold/weight 小调。
3. 不继续 SPECTRA boundary-energy / rank-guard 权重小调。
4. 新 idea 不能是 kNN/PPR/CAST mining、positive mining、score fusion、loss-only reweighting、post-hoc relation smoothing、单纯 negative-free + diagnostic regularizer。
5. 必须解释为什么 pair-level 和 representation-diagnostic-level 修正都无法转化为节点分类 accuracy。
6. 从“训练目标是否和少标签 Logistic Regression 下游匹配”重新定义问题。

## Fresh Reviewer Reading

Fresh `gcl_scientific_reviewer` 的核心意见：

- CAST、BEACON、SPECTRA 都在优化代理变量，而不是 frozen Logistic Regression 真实需要的全局线性可分结构。
- pair overlap、label agreement、margin/rank、effective-rank、boundary-retention 以后只能作为 diagnostics，不能作为 GO 条件。
- 下一轮应优先考虑“少标签校准的 evaluator-aligned GCL”，但必须诚实标注为 semi-supervised / train-label-calibrated，不得伪装成无监督。
- 推荐方向是 probe-aligned mechanism：把 false-negative 处理从 pair purity 改成“是否改善 train/val linear probe margin 与泛化稳定性”。

Fresh `gcl_experiment_auditor` 的核心约束：

- 新 idea 的 GO 条件必须是 frozen encoder + LogReg test@best-val 超过强 controls，不能是 overlap、agreement、margin/rank、effective-rank 或 boundary-retention。
- Cora seed0 smoke 未过 kill rules 前，不允许 Cora seeds 0-2、PubMed、Pilot-B、formal 或 GPU 长跑。
- 下一轮 runner 必须保存完整 run command，并尽量在 clean commit 下运行；raw JSON 中若 `project_dirty=true`，不得进入 formal/provenance claim。

## Why Previous Fixes Failed

### CAST

CAST 的 C4 latent certificate 与 kNN/PPR/CAST overlap 低，但 C4/C5 没有稳定超过 mining controls。低 overlap 只能证明“不是同一批 pair”，不能证明这些 pair 会改善少标签 linear decision boundary。

### BEACON

BEACON full gate 的 label agreement 和 margin/rank diagnostics 更好，但 accuracy 低于 shuffled/no-boundary/no-geometry controls。这说明“更像同类”或“局部几何更安全”的 pair edit 未必帮助真正的边界节点。

### SPECTRA

SPECTRA full 的 effective rank 和 boundary retention 高于 negative-free baseline，但 accuracy 更低。这说明 representation diagnostics 也可能只是在改善几何外观，而不是改善 Logistic Regression 可读出性。

## New Problem Statement

不再问：

> Which false-negative pairs should be repaired?

也不再问：

> Which representation diagnostic should be regularized?

改问：

> Can pretraining be calibrated so that the frozen representation is easier for a small train-label Logistic Regression probe to read out, without using test labels and without converting unlabeled nodes into pseudo-positive pairs?

## Ranked Ideas

完整候选见 `idea-stage/IDEA_CANDIDATES_20260626_160634.md`。本轮生成 7 个候选：

1. **PACER-GCL / Probe-Aligned Calibration for Evaluator-Ready GCL**
2. **MARS-GCL / Masked Readout-Sufficient Pretraining**
3. **VALOR-GCL / Validation-Oriented Objective Selection**
4. **CLASSIC-GCL / Class-Basis SSL without Pair Mining**
5. **GRAIL-GCL / Granularity-Regularized Linear Readout Alignment**
6. **AUGUR-GCL / Readout-Calibrated Augmentation Risk**
7. **FALCON-GCL / Fold-Consistent SSL Objective Routing**

## Selected Top1: PACER-GCL

**One-sentence thesis**：PACER-GCL uses train-label logistic probes to estimate which anchors are fragile for the downstream linear readout, then changes their SSL objective type from instance discrimination to masked logit-preserving consistency, avoiding pair mining while aligning pretraining with the actual node-classification evaluator.

### Core Mechanism

PACER has three stages:

1. **Warmup**：train a normal GRACE encoder for a short fixed budget。
2. **Probe calibration**：freeze warmup embeddings and train a Logistic Regression probe using train labels only。Validation labels can select fixed hyperparameters or early stopping, but cannot train the probe objective or thresholds used on test。
3. **Objective routing**：
   - readout-stable anchors：continue normal GRACE objective；
   - readout-fragile anchors：switch to masked logit-preserving consistency。Two masked views must preserve the train-probe logit direction/margin instead of being pushed through all-node instance discrimination。

Important：PACER does **not** select cross-node positives, does **not** smooth relations, and does **not** score unlabeled node pairs as same-class.

### Why It Addresses False Negatives

False negatives hurt because instance discrimination may optimize away information that a small linear classifier needs. PACER does not try to identify false-negative pairs. It detects anchors for which the current representation is fragile under the downstream readout and changes the pretraining treatment for those anchors. This reframes false negatives as an **objective-treatment mismatch** problem rather than a pair-classification problem.

### Novelty Boundary

PACER must not claim novelty for:

- supervised contrastive learning；
- semi-supervised GCL；
- simple probe loss；
- validation-based model selection；
- sample reweighting。

The defensible boundary is narrower:

> evaluator-aligned SSL objective routing for node-level GCL, where train-label probes decide objective type rather than pair labels, thresholds, relation smoothing, or scalar weights.

## Minimal Smoke

`PA-CER-M0-001`：

- Dataset：Cora seed0
- Split：stratified random `1:1:8`
- Evaluator：frozen encoder + Logistic Regression train/val `C` selection
- Status：smoke only

### Variants

| ID | Variant | Purpose |
|---|---|---|
| P0 | GRACE | baseline |
| P1 | probe-only diagnostic | train probe on warmup, no encoder update |
| P2 | mask-only consistency | masked consistency without probe routing |
| P3 | PACER full | train-probe objective routing + masked logit-preserving consistency |
| P4 | shuffled-label probe | leakage/story control |
| P5 | random-probe route | route control |
| P6 | scalar reweight control | prove not loss-only reweighting |
| P7 | train-only CV probe | avoid val leakage and check stability |
| P8 | supervised-contrastive small-label control | check whether PACER is merely SupCon/semi-supervised contrastive |

### Required Diagnostics

- accuracy / val accuracy / selected `C`
- exact run command and clean/dirty provenance
- train-probe margin delta
- validation-probe margin delta
- fragile-anchor fraction
- route distribution by degree bucket
- shuffled-label and random-route gaps
- train-vs-val overfit gap

## Decision

`GO_TO_PACER_SMOKE_PLANNING_WITH_CAUTION`

This decision is not a performance claim. It only means PACER is the best next smoke candidate after CAST/BEACON/SPECTRA no-go. The method must be described as train-label-calibrated / semi-supervised GCL, not unsupervised GCL.
